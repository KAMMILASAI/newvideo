# Production Configuration
spring.application.name=video-call-app
server.port=8080

# MongoDB Configuration (Atlas Cloud)
spring.data.mongodb.uri=mongodb+srv://saikammila143:sai143@cluster0.cozih.mongodb.net/videocalldb?retryWrites=true&w=majority&appName=Cluster0

# CORS Configuration - Production
spring.web.cors.allowed-origins=https://smarthirexvideocall.netlify.app
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# WebSocket Configuration - Production
spring.websocket.enabled=true
spring.websocket.allowed-origins=https://smarthirexvideocall.netlify.app

# Logging Configuration
logging.level.com.video.demo=INFO
logging.level.org.springframework.web=INFO

# Actuator for health checks
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
